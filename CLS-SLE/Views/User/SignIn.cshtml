@model CLS_SLE.Models.UserSignIn
@{
    ViewBag.Title = "Sign In";
}

<div class="font-md"><h2>Sign In</h2></div>
<div class="well">
    <form method="post" class="form-horizontal">
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <!-- PersonID -->
        <div class="form-group">
            @Html.LabelFor(model => model.IdNumber, "Username", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.TextBoxFor(model => model.IdNumber, new { @class = "form-control", placeholder = "Enter UserID" })
                @Html.ValidationMessageFor(model => model.IdNumber, "", new { @class = "text-danger" })
            </div>
        </div>
        <!-- Password -->
        <div class="form-group">
            @Html.LabelFor(model => model.Hash, "Password", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.PasswordFor(model => model.Hash, new { @class = "form-control", placeholder = "Enter Password" })
                @Html.ValidationMessageFor(model => model.Hash, "", new { @class = "text-danger" })
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <button type="submit" class="btn btn-default">Sign In</button>
            </div>
            <br />
            <a href="~/User/PasswordReset">Forgot password?</a>
        </div>
    </form>
</div>


@section scripts{
    @Scripts.Render("~/bundles/jqueryval")
}